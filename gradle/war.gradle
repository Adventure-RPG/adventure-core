apply plugin: "war"

bootWar {
    mainClassName = "com.adventure.core.AdventureCoreApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
